import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class TimeMachine here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class TimeMachine extends Actor
{
    GreenfootImage timeMachine = new GreenfootImage("4-41347_time-machine-time-travel-machine-clipart-removebg-preview.png");
    private int remainingTime = 600;
    /**
     * Act - do whatever the TimeMachine wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        //timeMachine.scale(150, 110);
        //setImage(timeMachine);
        //remainingTime--;
        //if(remainingTime % 63 == 0)
        //{
            // Timer has reached zero, perform some action here
            // For example, remove the TimeMachine from the world
           // getWorld().removeObject(this);
            //getWorld().removeObject(this);
            //setLocation(Greenfoot.getRandomNumber(1024),Greenfoot.getRandomNumber(400));
        //}
        
        //if(this.isTouching(Pedestrian.class))
        //{
        //    Greenfoot.setWorld(new DinoWorld());
        //}
    }
    
}  
